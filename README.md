# SQL-Problem
Task week 5

## problem 1
```sql
SELECT *
FROM CITY
WHERE COUNTRYCODE = 'USA' 
  AND POPULATION > 100000;
```
## problem 2
``` sql
SELECT NAME
 FROM Employee 
 ORDER BY NAME;
  ```
## problem 3
``` sql
SELECT tweet_id
FROM Tweets
WHERE LENGTH(content) > 15;
```

## problem 4
I can’t find this problem in the link sent in Week 5
